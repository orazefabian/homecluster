apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: speedtest-pvc
  namespace: halo
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi  
