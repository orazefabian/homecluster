apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ddclient-config
  namespace: halo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: longhorn # Use Longhorn storage class


