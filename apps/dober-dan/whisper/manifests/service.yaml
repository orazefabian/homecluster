apiVersion: v1
kind: Service
metadata:
  name: dober-dan-whisper
  namespace: halo
spec:
  type: ClusterIP
  selector:
    app: dober-dan-whisper
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP
